@model LuzBeer.Models.LuzBeer

@{
    ViewBag.Title = "Luz Beer";
}

<div id="hero" class="carousel slide carousel-fade section no-padding" data-ride="carousel">

    @*<ol class="carousel-indicators">
            <li data-target="#hero" data-slide-to="0"></li>
            <li data-target="#hero" data-slide-to="1"></li>
            <li data-target="#hero" data-slide-to="2"></li>
        </ol>*@
    <div class="carousel-inner" id="indexteste">
        <div class="item active parallax" style="">
            <div class="container">
                <div class="row blurb scrollme animateme" data-when="exit" data-from="0" data-to="1" data-opacity="0" data-translatey="100">
                    <div class="col-md-10 col-md-offset-1">
                        <img src="~/images/logo.png" class="logoPresentation" />
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@Html.Partial("_NossaHistoria")

@Html.Partial("_CervejaFixa", Model.CervejaFixa)

@Html.Partial("_KitDoMes", Model.Kit)

@Html.Partial("_NossaCerveja", Model.ListaCervejas)

@Html.Partial("_Contato")

@Html.Partial("_NewsLetter")
<style>
    .parallax {
        /* The image used */
        background-image: url('../images/imagemFundoPrincipal.jpg');
        /* Set a specific height */
        min-height: 500px;
        /* Create the parallax scrolling effect */
        background-attachment: fixed;
        background-position: center;
        background-repeat: no-repeat;
        background-size: cover;
    }

    .bs-carousel img {
        box-shadow: none !important;
        border: none !important;
    }
</style>
